# 🤖 RAG Chat Bot MVP

Полноценный MVP чат-бота на Flask с RAG (Retrieval-Augmented Generation), векторной памятью и локальной LLM моделью Mistral через Ollama.

## ✨ Особенности

- 🧠 **Локальная LLM** - Mistral через Ollama API
- 📚 **RAG функциональность** - поиск в загруженных документах
- 💾 **Векторная память** - долгосрочная память в рамках сессии
- 🔍 **FAISS поиск** - быстрый векторный поиск
- 🗂️ **Поддержка форматов** - TXT, PDF, DOCX
- 🌐 **Веб-интерфейс** - удобный HTML чат
- 🔒 **Локальная обработка** - полная конфиденциальность

## 🏗️ Архитектура

```
RAG Chat Bot
├── app.py              # Flask веб-сервер
├── llm_engine.py       # Вызов Mistral через Ollama API
├── memory.py           # Векторная память сессий
├── retriever.py        # Загрузка документов, FAISS поиск
├── utils.py            # Предобработка текста и эмбеддинги
├── requirements.txt    # Python зависимости
├── data/
│   └── docs/          # Ваши документы (TXT, PDF, DOCX)
├── vectorstore/       # FAISS индексы
├── static/           # CSS и JavaScript
└── templates/        # HTML шаблоны
```

## 🚀 Быстрый старт

### 1. Предварительные требования

- **Python 3.10+**
- **Ollama** ([скачать](https://ollama.ai/))
- **8GB+ RAM** (рекомендуется)

### 2. Установка Ollama и модели

```bash
# Установите Ollama для вашей системы
# Windows: скачайте с https://ollama.ai/
# Linux/Mac: curl -fsSL https://ollama.ai/install.sh | sh

# Запустите Ollama сервер
ollama serve

# В новом терминале загрузите модель Mistral
ollama pull mistral
```

### 3. Установка Python зависимостей

```bash
# Клонируйте репозиторий
git clone <your-repo-url>
cd rag-chat-bot

# Создайте виртуальное окружение
python -m venv .venv

# Активируйте окружение
# Windows:
.venv\Scripts\activate
# Linux/Mac:
source .venv/bin/activate

# Установите зависимости
pip install -r requirements.txt
```

### 4. Добавьте документы

```bash
# Скопируйте ваши документы в папку data/docs/
cp /path/to/your/documents/* data/docs/

# Поддерживаемые форматы: .txt, .pdf, .docx
```

### 5. Запуск

```bash
# Убедитесь что Ollama запущен (ollama serve)
# Запустите Flask сервер
python app.py
```

Откройте браузер: http://localhost:5000

## 🎯 Как это работает

### RAG Pipeline

1. **Пользователь отправляет сообщение**
2. **Поиск в документах** - находит релевантные фрагменты через FAISS
3. **Поиск в памяти** - находит релевантный контекст из истории чата
4. **Формирование промпта** - создает system prompt с контекстом
5. **Генерация ответа** - вызывает Mistral через Ollama API
6. **Сохранение в память** - добавляет сообщения в векторную память

### Векторная память

- Каждая сессия имеет свою изолированную память
- Сообщения пользователя и ассистента индексируются как эмбеддинги
- Поиск релевантного контекста для текущего запроса
- Память очищается при начале нового чата

### Обработка документов

- Автоматическое разбиение на чанки (500 символов с перекрытием)
- Очистка и нормализация текста
- Генерация эмбеддингов через sentence-transformers
- Индексирование в FAISS для быстрого поиска

## 🔧 Конфигурация

### Настройки в коде

```python
# utils.py - размер чанков
max_chunk_size = 500
overlap = 50

# llm_engine.py - параметры модели
temperature = 0.7
max_tokens = 2048

# retriever.py - модель эмбеддингов
embedding_model = "all-MiniLM-L6-v2"
```

### Переменные окружения

```bash
# Опционально - URL Ollama (по умолчанию localhost:11434)
export OLLAMA_BASE_URL=http://localhost:11434
```

## 📊 API Endpoints

- `GET /` - Главная страница чата
- `POST /send_message` - Отправка сообщения
- `GET /get_conversation` - История диалога
- `POST /clear_conversation` - Очистка чата и памяти
- `GET /check_status` - Статус системы
- `POST /refresh_documents` - Обновление индекса документов
- `POST /upload_document` - Добавление текста в индекс
- `GET /memory_stats` - Статистика памяти

## 🛠️ Устранение неполадок

### LLM недоступен
```bash
# Проверьте что Ollama запущен
ollama list

# Перезапустите Ollama
ollama serve
```

### Документы не индексируются
```bash
# Проверьте права доступа
ls -la data/docs/

# Попробуйте обновить индекс вручную
# В веб-интерфейсе нажмите "Обновить документы"
```

### Медленная работа
- Увеличьте RAM
- Используйте SSD диск
- Уменьшите количество документов

### Ошибки кодировки
- Сохраните документы в UTF-8
- Проверьте содержимое файлов

## 📝 Примеры использования

### Вопросы о документах
```
Пользователь: "Расскажи о возможностях RAG Chat Bot"
Система: Найдет релевантные фрагменты из документации
```

### Контекстные диалоги
```
Пользователь: "Какие форматы поддерживаются?"
Бот: "Система поддерживает TXT, PDF, DOCX форматы..."

Пользователь: "А как их добавить?"
Бот: [использует контекст предыдущего вопроса]
```

## 🔄 Обновления и расширения

### Добавление новых документов
1. Скопируйте файлы в `data/docs/`
2. Нажмите "Обновить документы" в интерфейсе
3. Или перезапустите сервер

### Смена модели LLM
```bash
# Загрузите другую модель
ollama pull llama2

# Измените в app.py или выберите в интерфейсе
```

### Настройка эмбеддингов
Измените `embedding_model` в `utils.py`:
- `all-MiniLM-L6-v2` - быстрая, 384 измерения
- `paraphrase-multilingual-MiniLM-L12-v2` - мультиязычная
- `all-mpnet-base-v2` - более точная, 768 измерений

## 📋 TODO / Планы развития

- [ ] Поддержка файлового uploads через веб-интерфейс
- [ ] Экспорт/импорт памяти сессий
- [ ] Настройки через веб-интерфейс
- [ ] Поддержка изображений и таблиц
- [ ] Интеграция с другими LLM провайдерами
- [ ] Многопользовательский режим с аутентификацией

## 🤝 Вклад в проект

1. Fork репозитория
2. Создайте feature branch
3. Сделайте изменения
4. Добавьте тесты
5. Создайте Pull Request

## 📄 Лицензия

MIT License - используйте свободно для личных и коммерческих проектов.

## 🆘 Поддержка

При возникновении проблем:
1. Проверьте секцию "Устранение неполадок"
2. Посмотрите логи в консоли
3. Создайте Issue с описанием проблемы

---

**Готово к использованию!** 🚀

Теперь у вас есть полноценный RAG чат-бот с векторной памятью и поиском в документах, работающий полностью локально. 